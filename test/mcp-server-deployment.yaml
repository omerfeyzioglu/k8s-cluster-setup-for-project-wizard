apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-server-test
  template:
    metadata:
      labels:
        app: mcp-server-test
    spec:
      containers:
      - name: mcp-server
        # CI pipeline'ınız bu satırı güncelleyecek
        image: docker.io/omerfeyzioglu/projectwizard-mcp-server:develop-c1b5170
        ports:
        - containerPort: 8081 # Dockerfile'da 8081 demiştik